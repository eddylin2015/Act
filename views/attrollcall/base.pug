doctype html
html
  head
    title act
    meta(charset='utf-8')
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel='stylesheet', href='/stylesheets/bootstrap.min.css')
    link(rel='stylesheet', href='/stylesheets/main.css')
    script(src='/javascripts/jquery.js')    
    style.
      a {color:white;}
  body
    .navbar.navbar-default
      .container
        .navbar-header
          .navbar-brand 點名登記
        ul.nav.navbar-nav
          li
            a(href="/internal/") 
              span(class="glyphicon") &#xe021
          li
            a(href="/internal/attrollcall") 班別
          li
            a(href="/internal/attrollcall?sect=Dept") 部門

          if att_pass  
            li
              a(href="/internal/attrollcall/myrollcall") 職員登記
                      
          if act_c_id
            li
              a(href=`/internal/attrollcall/al_list/${act_c_id}?fn=${fn}`) 項目(#{act_c_id})            

          //li
            a(href="/internal/markup/act/actlist") 活動列表
          //li
            a(href="/internal/markup/act/cnolist?cno=P") 小學活動分
          //li
            a(href="/internal/markup/act/cnolist?cno=S") 中學活動分

        p.navbar-text.navbar-right
          if att_pass
            a(href="/internal/attrollcall/al_logout",style="color:green") (exit)#{att_pass.stud_ref}            
          if profile
            if profile.image
              img.img-circle(src=profile.image, width=24)
            span #{profile.displayName} &nbsp;
              //a(href='/logout') (logout)
          else
            //a(href='/login') Login
    .container
      block content
